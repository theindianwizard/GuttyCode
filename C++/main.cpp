#include <iostream>
using namespace std;
#include "function.cpp"

void BigDog(int KibblesCount);

int main() {
  BigDog(3);
  return 0;
  for (std::vector<>::iterator i = .begin(); i != .end(); ++i)
  {
    
  }
}